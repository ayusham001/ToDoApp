<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do App</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            width: fit-content;
            margin: 50px auto;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 20px 20px rgba(0, 0, 0, 0.436);
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .todo-list {
            list-style-type: none;
            padding: 0;
        }

        .todo-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background-color: #f8f8f8;
            padding: 10px;
            margin: 5px 0;
            border-radius: 4px;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
        }

        .todo-item:before {
            content: "\2022";
            margin-right: 10px;
            color: #333;
        }

        /* Optional: Hover effect */
        .todo-item:hover {
            background-color: #f0f0f0;
            cursor: pointer;
        }

        .task-container {
            display: flex;
        }

        .task-container .tasks {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            width: fit-content;
            border: 1px solid #333;
            padding: 10px;
            margin-right: 10px;
        }

        .task-container .task {
            display: flex;
            justify-content: space-between;
            gap: 50px;

        }

        .task-container .task-list {
            margin: 0;
            padding: 0;
            display: flex;
        }

        .task-container .imagediv .todo-image{
            width: 50px;
            height: 50px;
        }

        .task-container .task-action{
            display: flex;
            align-items: center;
        }

        .task-container .tasks #delete {
            cursor: pointer;
            height: fit-content;
        }

        .task-container .task-adder {
            display: flex;
            flex-direction: column;
            height: fit-content;
            border: 1px solid #333;
            padding: 10px;
        }

        .task-container .task-adder .down {
            padding-top: 12px;
            display: flex;
            flex-direction: column;
            gap: 5px;
        }

        .task-container .task-adder .down #button {
            width: 100px;
        }
    </style>
</head>

<body>
    <!-- Other content for logged-in users -->
    <%- include('header') %>
        <div class="container">
            <h1>Ultimate To-Do List</h1>
            <div class="task-container">
                <div class="tasks" id="taskList">
                    <p style="margin: 0; padding: 0;text-decoration: underline; font-size: larger;text-align: center;">
                        Tasks</p>
                </div>
                <div class="task-adder">
                    <form id="taskForm" enctype="multipart/form-data">
                        <input id="ip" placeholder="Enter Task" />
                        <div class="down">
                            <input type="file" id="task_pic" name="image" />
                            <input id='button' type="submit" value="Submit" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <script src="script.js"></script>
</body>

</html>